<%= form_for @tale, html: { class: "well" } do |f| %>

  <%= f.label :title %><br />
  <%= f.text_field :title %><br />

  <%= f.label :body %><br />
  <%= f.text_area :body, cols: 60, rows: 10%>

  <br />
  <%= f.submit %>

<% end %>
